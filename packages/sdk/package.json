{
  "name": "@mainframe/sdk",
  "version": "0.1.0",
  "main": "lib/index.js",
  "license": "MIT",
  "scripts": {
    "clean": "del lib && del cjs",
    "build:js": "yarn webpack",
    "build:cjs": "BABEL_ENV='browser-cjs' babel src --out-dir cjs",
    "build:flow": "flow-copy-source src lib",
    "build": "yarn clean && yarn build:js && yarn build:cjs && yarn build:flow",
    "test:types": "flow check"
  },
  "devDependencies": {
    "@mainframe/rpc-stream": "^0.2.4",
    "babel-preset-flow": "^6.23.0",
    "babel-loader": "^7.1.5",
    "babel-plugin-external-helpers": "^6.22.0",
    "flow-bin": "^0.79.1",
    "webpack": "^4.17.0",
    "webpack-cli": "^3.1.0"
  }
}
