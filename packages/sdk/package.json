{
  "name": "@mainframe/sdk",
  "version": "0.1.0",
  "main": "cjs/index.js",
  "license": "MIT",
  "scripts": {
    "clean": "del cjs && del dist",
    "build:dist": "BABEL_ENV='browser-esm' yarn webpack",
    "build:cjs": "BABEL_ENV='browser-cjs' babel src --out-dir cjs",
    "build:flow": "flow-copy-source src cjs",
    "build:sdk": "yarn clean && yarn build:dist && yarn build:cjs && yarn build:flow",
    "test:types": "flow check"
  },
  "dependencies": {
    "@babel/runtime": "^7.0.0",
    "web3-provider-engine": "^14.1.0"
  },
  "devDependencies": {
    "@mainframe/rpc-stream": "^0.2.4",
    "babel-loader": "^8.0.0",
    "flow-bin": "^0.81.0",
    "webpack": "^4.19.1",
    "webpack-cli": "^3.1.0"
  }
}
