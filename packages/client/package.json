{
  "name": "@mainframe/client",
  "version": "0.1.0",
  "main": "lib/index.js",
  "license": "MIT",
  "scripts": {
    "clean": "del lib",
    "build:js": "flow-remove-types src --out-dir lib",
    "build:flow": "flow-copy-source src lib",
    "build": "yarn clean && yarn build:js && yarn build:flow",
    "test": "flow check"
  },
  "dependencies": {
    "@mainframe/rpc-ipc": "^0.2.0",
    "@mainframe/utils-base64": "^0.1.0",
    "rxjs": "^6.1.0",
    "sodium-universal": "^2.0.0"
  },
  "devDependencies": {
    "@mainframe/rpc-stream": "^0.2.1",
    "flow-bin": "^0.73.0"
  }
}
